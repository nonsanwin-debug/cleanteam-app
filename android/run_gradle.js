const { execSync } = require('child_process'); try { const stdout = execSync('.\\gradlew.bat assembleDebug', { encoding: 'utf8', env: {...process.env, JAVA_HOME: 'C:/Program Files/Android/Android Studio/jbr'} }); console.log('SUCCESS'); console.log(stdout.slice(-2000)); } catch (e) { const fs = require('fs'); fs.writeFileSync('gradle_error.log', e.stdout || e.message); console.log('FAILED, wrote log: ' + e.message); }
